#spring.application.name=QuizLand
#
#spring.datasource.url=jdbc:postgresql://localhost:5432/quiz_land
#spring.datasource.username=postgres
#spring.datasource.password=1
#spring.datasource.driver-class-name=org.postgresql.Driver
#
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#
#
#
#spring.jpa.properties.hibernate.default_schema=quiz_land


spring.application.name=QuizLand

# Use environment variables for     DB connection; fallback to localhost for local dev
#spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/quiz_land}
#spring.datasource.username=${DATABASE_USERNAME:postgres}
#spring.datasource.password=${DATABASE_PASSWORD:1}
#spring.datasource.driver-class-name=org.postgresql.Driver
#
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#
#spring.jpa.properties.hibernate.default_schema=quiz_land

# Let Render control the port, fallback to 8080 locally
#server.port=${PORT:8080}


# Use Railway's JDBC environment variables
spring.datasource.url=${DATABASE_PUBLIC_URL}
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Optional: show SQL statements in logs (for debugging)
spring.jpa.show-sql=true
